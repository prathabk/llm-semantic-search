# Semantic Search LLM - Environment Variables
# Copy this file to .env and update with your values

# Typesense Configuration
TYPESENSE_HOST=localhost
TYPESENSE_PORT=8108
TYPESENSE_API_KEY=vL1l1TOq2UYhPxKqJfvfWXvm0wIID6se

# Collection Names (can be customized)
COLLECTION_NAME=llm-semantic-search

# Flask Configuration
FLASK_ENV=development
FLASK_DEBUG=True

# Note: For production use, change the TYPESENSE_API_KEY to a secure, randomly generated value
# You can generate a secure key with: openssl rand -base64 32
